# =============================================================================
# GymGurus — Docker Ignore
# =============================================================================
# Keep the Docker build context small and exclude sensitive/unnecessary files.
# =============================================================================

# Dependencies (reinstalled inside container)
node_modules
npm-debug.log*

# Build output (rebuilt inside container)
dist

# Environment files (secrets — never bake into images)
.env
.env.*
!.env.example

# Git
.git
.gitignore

# IDE / Editor
.vscode
.idea
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db
nul

# Documentation (not needed at runtime)
docs
*.md
LICENSE

# Test files
**/*.test.ts
**/*.test.tsx
**/*.spec.ts
**/*.spec.tsx
**/__tests__
server/test
server/routes.test.ts.skip
coverage
.nyc_output

# Docker files (prevent recursive context)
Dockerfile
docker-compose.yml
docker-compose.*.yml
.dockerignore

# Miscellaneous
*.tar.gz
*.tgz
cookies.txt
gym-gurus/
.local/
.claude/
.replit
replit.nix
